import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.induz.bleAds',
  appName: 'bleAds',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
